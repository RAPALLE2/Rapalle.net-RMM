# RAPALLE.net RMM - Backend Abhängigkeiten
# Installieren mit: pip install -r requirements.txt
#
# Untere Grenzen (>=) statt fester Versionen, damit pip eine stimmige
# Kombination wählt (verhindert z.B. aiohttp/socketio-Versionskonflikte).

fastapi>=0.115.0          # Web-Framework für die REST-API
uvicorn[standard]>=0.32.0 # ASGI-Server, der FastAPI ausführt
python-socketio>=5.11.0   # Echtzeit-Verbindungen (Agenten + Dashboard)
python-dotenv>=1.0.0      # Liest .env Dateien für Konfiguration
bcrypt>=4.2.0             # Sicheres Passwort-Hashing (direkt, ohne passlib)
PyJWT>=2.9.0              # Login-Tokens (JWT)
paramiko>=3.4             # SSH-Teil fuer den SFTP-Zugang am Relay (optional -
                          # ohne das Paket bleibt nur FTP waehlbar)
qrcode>=7.4               # QR-Code fuer die Zwei-Faktor-Einrichtung. Fehlt es,
                          # zeigt die Oberflaeche das Geheimnis zum Abtippen.
python-multipart>=0.0.9   # Datei-Uploads und Formulare (FastAPI braucht es fuer
                          # UploadFile/Form - ohne das startet die App NICHT,
                          # sobald ein Router einen Upload anbietet)
pydantic>=2.9.0          # Datenvalidierung für die API
psutil>=6.0.0            # Laufwerksauflistung für "dieser Server" im Datei-Explorer
cryptography>=42.0.0    # Verschlüsselt das Konto-Passwort für den Explorer-Relay (Digest/WebDAV)
Pillow>=10.0.0         # Bildverarbeitung für die (optionale) serverseitige Screen-Aufnahme
ldap3>=2.9.0            # LDAP/Active-Directory-Anmeldung (Realms). Optional: ohne
                        # dieses Paket funktioniert nur die lokale Anmeldung.

# --- Optional, EXPERIMENTELL ---
# pyrdp aktiviert das eingebettete RDP-Streaming für headless VMs (Button
# "Im Browser versuchen"). Es ist ein Security-Forschungswerkzeug und KEIN
# polierter RDP-Client - das Bild kann unvollständig sein oder abbrechen.
# Der zuverlässige Weg ist immer der native ".rdp"-Download (mstsc).
# Zum Aktivieren die folgende Zeile einkommentieren:
# pyrdp-mitm>=2.1.0

# --- Optional: Externe SQL-Datenbank (Settings -> Datenbank) ---
# Nur nötig, wenn auf eine externe Datenbank umgeschaltet werden soll:
# PyMySQL>=1.1.0          # MySQL / MariaDB
# psycopg2-binary>=2.9.0  # PostgreSQL
